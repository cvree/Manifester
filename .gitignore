node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
.env
.env.*
!.env.example
*.log
npm-debug.log*
.idea
.vscode/*
!.vscode/extensions.json
dev-dist
data

# Kokoro weights and the Python environment `npm run speech:local` builds.
# Both are ~400 MB of things that can be fetched again; the audio they produce
# is what belongs in the repository.
.cache
